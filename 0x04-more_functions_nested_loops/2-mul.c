#include "main.h"

/**
 * mul - multiplication function
 * @a: first variable to work with
 * @b: second variable to work with
 *
 * Return: returns 1 if uppercase, 0 otherwise
 */

int mul(int a, int b)
{
	int retVal = a * b;

	return (retVal);
}
