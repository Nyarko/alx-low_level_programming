#include <stdio.h>
/**
 * _puts - function to print string to stdout
 * @str: variable to be printed to stdout
 * puts - function from stdlib
 * Return: no return for void function
 */

void _puts(char *str)
{
	puts(str);
}
